/**
 * Native pixels of `public/images/map-emotional-base.jpg` (map + route gaps only; marker icons drawn in SVG).
 *
 * Figma “Emotional map” frame (for coordinates): 8045 × 19145 — see `markers.ts`.
 */
export const MAP_NATIVE_WIDTH = 430;
export const MAP_NATIVE_HEIGHT = 1024;

/** Scale Figma Emotional map pixels → app viewBox (same aspect ratio as 430×1024). */
export const FIGMA_MAP_WIDTH = 8045;
export const FIGMA_MAP_HEIGHT = 19145;
